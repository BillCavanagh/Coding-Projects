/* Data types (primatives)
* undefined, null, boolean, string, symbol (immutable unique primative), number and BigInt (float)
*/

var num = 1; // var = global
let string = "string"; // let = local
const bool = true; // const = constant

/* Operations
* addition = + 
* subtraction = - 
* multiplication = *
* exponentiation = **
* division = / (note bc there are no floats, this is decimal division not integer division)
* remainder = %
* increment ++ (both pre and post)
* decrement -- (both pre and post)
* can use operations in assignment for example: num += 5; num %= 2; ect.
*/

let num1 = 5;
let num2 = 2;
console.log(num1/num2);